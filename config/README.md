Create a file called `settings.js` with the following values substituted:

```json
module.exports = {
  github : {
    CLIENT_ID: '<your github CLIENT_ID>',
    SECRET_KEY: '<your github SECRET_KEY>'
  },
  session : {
    secret: '<choose a session secret>'
  }
}```
